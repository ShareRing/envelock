{
  "name": "envelock-reference-backend",
  "version": "0.1.0",
  "private": true,
  "description": "Reference material endpoint for envelock: verifies a host JWT against its JWKS and releases a per-user pepper.",
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "start": "node --experimental-strip-types src/server.ts",
    "typecheck": "tsc --noEmit",
    "test": "node --experimental-strip-types --test src/*.test.ts"
  },
  "dependencies": {
    "jose": "^5.9.6"
  },
  "devDependencies": {
    "@types/node": "^22.7.0",
    "typescript": "^5.6.0"
  }
}
